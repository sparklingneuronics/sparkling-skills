# Local, per-user Claude Code settings — not part of the published marketplace.
.claude/settings.local.json

# Internal working docs (growth strategy, in-progress specs, AI research) — never published.
private/

# Local Claude Code state — personal commands/settings, not part of the published marketplace.
.claude/
.DS_Store
